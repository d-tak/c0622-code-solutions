/* exported getValue */

/* GOAL: to return the value in object that is found at key.
1. Create a function (given)
2. Find the value of object at key.
3. Return the key property of the object.
*/

function getValue(object, key) {
  return object[key];
}
