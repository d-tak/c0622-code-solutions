/* exported setValue */

/* GOAL: to return nothing - undefined.
1. Create a function (given)
2. assign a value to the key of the object
3. return nothing!! (undefined)
evaluate in dev tools.
*/

function setValue(object, key, value) {
  object[key] = value;
}
