/* exported getFirstChar */

/* GOAL: identify the first character of each string and return its value.
1. Create a function with one parameter, string (GIVEN)
2. Find the first letter of any given JS string (How? Use a method.)
   return 'cat'[0] // returns "c"
3. Return the first character of the string.

*/

function getFirstChar(string) {
  return string[0];
}
