/* exported sumAll */
function sumAll(numbers) {
  // var numberTotal = [];
  // var currentNumber = number;
  // while (currentNumber >= 0) {
  //   numberList.push(currentNumber);
  //   currentNumber--;
  // }
  // return numberTotal;
}
